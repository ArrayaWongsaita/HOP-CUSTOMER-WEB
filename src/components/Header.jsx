import React from "react";

const Header = () => (
  <div className="bg-blue-500 w-full h-[70px] flex items-center justify-center fixed top-0 left-0 z-50">
    <h1 className="text-3xl font-bold">Höp</h1>
  </div>
);

export default Header;
